import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    mainView: {
        height: '100%',
        width: '100%',
        backgroundColor: '#AAA'
    }
});

export default styles;